# Self-signed certificate for local development
# Run the following command in PowerShell to generate:
# openssl req -x509 -nodes -days 365 -newkey rsa:2048 -keyout key.pem -out cert.pem -subj "/CN=localhost"
